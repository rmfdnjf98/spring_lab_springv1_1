# Spring Boot - 면접 문장 속 용어 완전 정리

> 📄 노션 페이지 제목 / 파일명 추천: `Spring Boot - 면접 용어 완전 정리`

---

## 1. 주제

면접/이력서에 쓰인 문장 속 낯선 용어들을 하나씩 쪼개서 이해한다.
용어를 알면 문장이 무슨 말인지 자연스럽게 읽힌다.

---

## 2. 용어 지도 — 전체 관계 한눈에 보기

```
Spring Boot 프로젝트
│
├── 화면(View)을 만드는 기술
│   ├── Mustache  → 예전에 쓰던 방식 (간단하지만 기능 제한)
│   └── JSP + JSTL → 새로 바꾼 방식 (전통적인 Java 웹 기술)
│        └── 이 교체 작업 = "마이그레이션"
│
├── DB와 연결하는 기술
│   ├── JPA  → SQL을 자동으로 만들어주는 방식
│   │    └── JPQL = JPA가 쓰는 전용 쿼리 문법
│   └── MyBatis → SQL을 직접 내가 쓰는 방식
│        └── SQL을 XML 파일에 직접 작성
│
└── 화면 경로를 찾아주는 설정
    └── ViewResolver (prefix + suffix로 JSP 파일 경로 완성)
        └── EL (${ }) = 화면에 데이터 출력할 때 쓰는 문법
```

---

## 3. 용어별 개념 정리

---

### 용어 1. Spring MVC

**한 줄 정의:** 사용자 요청을 받아서 처리하고 화면으로 돌려주는 Spring의 웹 처리 구조

MVC는 Model, View, Controller의 약자다.

| 역할 | 하는 일 | 예시 |
|---|---|---|
| **Model** | 데이터 | DB에서 꺼낸 게시글 목록 |
| **View** | 화면 | 게시글을 보여주는 JSP 파일 |
| **Controller** | 연결 | 요청 받아서 데이터 조회 후 화면에 전달 |

**비유:** 식당에 비유하면,
- 손님(사용자)이 주문(요청)하면
- 홀 직원(Controller)이 받아서 주방(Model)에 전달하고
- 완성된 음식(View)을 다시 손님에게 가져다 준다

---

### 용어 2. 뷰(View) 기술

**한 줄 정의:** 사용자에게 보여주는 화면을 만드는 방법/도구

Spring Boot에서 화면을 만드는 방법이 여러 개 있다.
그 중 어떤 걸 선택해서 쓸지가 "뷰 기술 선택"이다.

| 뷰 기술 | 특징 |
|---|---|
| **Mustache** | 문법 단순, Spring Boot 기본 지원 |
| **JSP + JSTL** | Java 전통 방식, 기능 풍부, 별도 설정 필요 |
| **Thymeleaf** | 현재 가장 많이 쓰는 방식 |

---

### 용어 3. Mustache (머스태시)

**한 줄 정의:** `{{ }}` 이중 중괄호를 사용해서 데이터를 화면에 넣는 간단한 템플릿 엔진

```html
<!-- Mustache 문법 예시 -->
<h1>{{ title }}</h1>
<ul>
  {{#boards}}
    <li>{{ title }}</li>
  {{/boards}}
</ul>
```

Spring Boot에서 의존성 하나만 추가하면 바로 쓸 수 있어서 입문용으로 많이 쓴다.
하지만 기능이 단순해서 복잡한 화면을 만들 때는 한계가 있다.

---

### 용어 4. JSP (Java Server Pages)

**한 줄 정의:** HTML 파일 안에 Java 코드를 넣어서 동적인 화면을 만드는 기술

```jsp
<!-- JSP 문법 예시 -->
<h1>${board.title}</h1>   <!-- EL 문법으로 데이터 출력 -->
```

서버에서 JSP 파일을 실행하면 Java 코드 부분이 처리된 후 순수한 HTML로 변환된다.
그 HTML이 브라우저로 전달된다.

⚠️ Spring Boot는 기본적으로 JSP를 지원하지 않는다.
`tomcat-embed-jasper` 라이브러리를 추가해야 JSP가 작동한다.

---

### 용어 5. JSTL (Jakarta Standard Tag Library)

**한 줄 정의:** JSP에서 Java 코드 없이 반복문, 조건문 등을 HTML 태그처럼 쓸 수 있게 해주는 태그 모음

JSTL 없이 JSP에서 반복문을 쓰려면 이렇게 해야 한다.
```jsp
<!-- JSTL 없이 (지저분함) -->
<% for(Board b : boards) { %>
  <li><%= b.getTitle() %></li>
<% } %>
```

JSTL을 쓰면 이렇게 바뀐다.
```jsp
<!-- JSTL 사용 (깔끔함) -->
<c:forEach var="b" items="${boards}">
  <li>${b.title}</li>
</c:forEach>
```

JSTL은 태그 라이브러리(tag library)라 불린다.
HTML 태그처럼 생겼지만 Java 로직을 실행하는 특수 태그다.

---

### 용어 6. EL (Expression Language)

**한 줄 정의:** JSP 화면에서 `${변수명}` 형태로 데이터를 출력하는 문법

```jsp
${board.title}    → board 객체의 title 필드값을 출력
${boards.size()}  → boards 목록의 크기를 출력
```

컨트롤러에서 `model.addAttribute("board", board)` 로 전달한 데이터를
JSP에서 `${board.title}` 처럼 꺼내서 쓴다.

**비유:** 컨트롤러가 데이터를 택배 상자(Model)에 넣어 보내면,
JSP에서 `${ }` 로 상자를 열어 꺼내 쓰는 것이다.

---

### 용어 7. 마이그레이션 (Migration)

**한 줄 정의:** 기존에 쓰던 기술을 다른 기술로 교체하는 작업

"Mustache에서 JSP+JSTL로 마이그레이션했다"는
→ 화면 기술을 Mustache에서 JSP+JSTL로 **직접 바꿨다**는 뜻이다.

마이그레이션은 단순히 파일만 바꾸는 게 아니다.
- 기존 코드 분석
- 새 기술로 재작성
- 설정 변경
- 동작 확인

이 모든 과정을 거친다. 그래서 면접에서 언급하면 좋은 경험이다.

---

### 용어 8. JPA (Java Persistence API)

**한 줄 정의:** Java 객체와 DB 테이블을 자동으로 연결해주는 기술. SQL을 직접 쓰지 않아도 된다.

```java
// JPA 사용 예시 - SQL 없이 DB 조작
List<Board> boards = boardRepository.findAll();  // SELECT * FROM board_tb 자동 생성
boardRepository.save(board);                     // INSERT 자동 생성
boardRepository.delete(board);                   // DELETE 자동 생성
```

Java 코드만 작성하면 JPA가 알아서 SQL을 만들어서 실행한다.
SQL을 잘 몰라도 기본적인 DB 조작이 가능하다는 장점이 있다.

⚠️ 복잡한 쿼리는 자동 생성이 어렵다. 그럴 때 MyBatis를 쓴다.

---

### 용어 9. MyBatis

**한 줄 정의:** SQL을 개발자가 직접 XML 파일에 작성하고, Java 메서드와 연결해주는 기술

```xml
<!-- MyBatis XML에 SQL 직접 작성 -->
<select id="findAll" resultType="Board">
    SELECT * FROM board_tb ORDER BY id DESC
</select>
```

JPA는 SQL 자동 생성, MyBatis는 SQL 직접 작성이다.
복잡한 조회 쿼리나 성능이 중요한 쿼리는 MyBatis로 직접 작성하는 게 유리하다.

| 비교 | JPA | MyBatis |
|---|---|---|
| SQL 작성 | 자동 생성 | 직접 작성 |
| 학습 난이도 | 높음 (개념 복잡) | 낮음 (SQL만 알면 됨) |
| 복잡한 쿼리 | 불편함 | 자유롭게 가능 |
| 실무 사용 | 빠른 개발에 적합 | 세밀한 제어에 적합 |

---

### 용어 10. JPQL (Java Persistence Query Language)

**한 줄 정의:** JPA에서 사용하는 특별한 쿼리 문법. SQL과 비슷하지만 테이블명 대신 Java 클래스명을 쓴다.

```java
// 일반 SQL
SELECT * FROM board_tb WHERE id = 1

// JPQL (테이블명 대신 Java 클래스명 사용)
SELECT b FROM Board b WHERE b.id = 1
//           ↑ Java 클래스 Board
```

JPQL은 DB 종류에 상관없이 동일하게 작동한다.
JPA가 JPQL을 각 DB에 맞는 실제 SQL로 변환해준다.

---

### 용어 11. ViewResolver (뷰 리졸버)

**한 줄 정의:** 컨트롤러가 반환한 문자열을 실제 파일 경로로 변환해주는 Spring의 설정

```properties
# application.properties 설정
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp
```

컨트롤러가 `"board/detail"` 을 반환하면 ViewResolver가 이렇게 경로를 완성한다.

```
/WEB-INF/views/  +  board/detail  +  .jsp
    (prefix)        (반환 문자열)     (suffix)
         ↓
/WEB-INF/views/board/detail.jsp  ← 이 파일을 찾아서 보여줌
```

**비유:** 택배 주소처럼, "board/detail"이라는 짧은 이름을 받아서
실제 전체 경로(주소)로 완성해주는 역할이다.

prefix(프리픽스) = 앞에 붙이는 것
suffix(서픽스) = 뒤에 붙이는 것

---

## 4. 면접 문장 다시 읽기

이제 다시 면접 문장을 읽으면 무슨 말인지 보인다.

---

**"Spring MVC의 뷰 기술을 Mustache에서 JSP+JSTL로 직접 마이그레이션했습니다"**

→ Spring의 웹 처리 구조(MVC)에서 화면을 만드는 방법을
→ Mustache(간단한 템플릿)에서 JSP+JSTL(Java 전통 방식)로
→ 직접 교체하는 작업을 해봤다는 뜻

---

**"JPA의 자동 SQL과 MyBatis의 직접 SQL 방식을 모두 경험했습니다"**

→ SQL이 자동 생성되는 JPA 방식도 써봤고
→ XML에 SQL을 직접 작성하는 MyBatis 방식도 써봤다는 뜻
→ 두 가지 DB 연결 방식의 차이를 실제로 경험했다는 의미

---

**"JPQL과 일반 SQL(MyBatis XML)의 차이점을 이해하고 있습니다"**

→ JPA 전용 쿼리 문법(JPQL: 클래스명 기반)과
→ 일반 SQL(테이블명 기반)을 MyBatis XML에 직접 쓰는 방식의 차이를 안다는 뜻

---

**"Spring MVC의 ViewResolver 설정(prefix/suffix)을 이해하고 있습니다"**

→ 컨트롤러가 반환한 짧은 문자열을 실제 JSP 파일 경로로 변환하는 설정을
→ application.properties에서 prefix와 suffix로 직접 해봤다는 뜻

---

**"EL(Expression Language)과 JSTL을 활용한 동적 뷰 개발 경험이 있습니다"**

→ JSP 화면에서 `${변수명}` 문법(EL)으로 데이터를 출력하고
→ `<c:forEach>` 같은 JSTL 태그로 반복/조건 처리를 해서
→ 데이터가 바뀌면 화면도 자동으로 바뀌는 동적인 화면을 만들어봤다는 뜻

---

## 5. 빠른 복습 포인트

> 이것만 봐도 용어가 기억난다

- **Spring MVC** = 요청 → Controller → Model → View 로 이어지는 웹 처리 구조
- **뷰(View) 기술** = 화면 만드는 방법. Mustache / JSP / Thymeleaf 중 선택
- **Mustache** = `{{ }}` 문법. 간단하지만 기능 제한
- **JSP** = HTML 안에 Java 코드 가능. 별도 라이브러리 필요
- **JSTL** = JSP에서 반복/조건을 태그로 쓸 수 있게 해주는 도구. `<c:forEach>`
- **EL** = JSP에서 `${변수명}` 으로 데이터 출력하는 문법
- **마이그레이션** = 기존 기술을 새 기술로 교체하는 작업
- **JPA** = SQL 자동 생성. Java 객체로 DB 조작
- **MyBatis** = SQL 직접 작성. XML에 쿼리 작성 후 Java 메서드와 연결
- **JPQL** = JPA 전용 쿼리. 테이블명 대신 Java 클래스명 사용
- **ViewResolver** = 컨트롤러 반환값 + prefix + suffix = 실제 JSP 경로 완성
